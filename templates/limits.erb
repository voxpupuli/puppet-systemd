# This file managed by Puppet - DO NOT EDIT
[Service]
<%
  @limits.keys.sort.each do |k|

    # Handles the Path and Option entries
    if @limits[k].is_a?(Array)
      output = @limits[k].map{|x|
        x_to_a = x.to_a.flatten
        x = %(#{k}=#{x_to_a.first} #{x_to_a[1..-1].join(' ')})
      }.join("\n")
   else
     output = %(#{k}=#{@limits[k]})
   end
-%>
<%= output %>
<% end -%>
